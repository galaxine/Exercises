/*Todo 1. has a time
       2. has a countdown
       3. has a state of being on or off.

 */

public class Lamp {
    private boolean lampState = true;
    private int timer;

    public Lamp(int timer) {
        this.timer = timer;
    }

    public boolean off(int timer) {
        if (timer == 0) {
            return false;
        } return false;
    }
}
